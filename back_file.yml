# - name: check if file is symlink
#   stat:
#     path: "{{ home }}/.bashrc"
#   register: file
# - name: Only when when file is not link
#   debug:
#     var: file
#   when: file.stat.islnk is defined and file.stat.islnk == false

# - name: check if file is symlink
#   stat:
#     path: "{{ item.path }}/{{ item.name }}"
#   loop: "{{ config_files }}"
#   register: file
# - name: Only when when file is not link
#   debug:
#     var: "{{ item.stat }}"
#   when: item.stat.islnk is defined and item.stat.islnk == false
#   loop: "{{ file.results }}"

# tasks file for dotfiles
# - name: get the username running the deploy
#   become: false
#   local_action: command echo $HOME
#   register: username_on_the_host
